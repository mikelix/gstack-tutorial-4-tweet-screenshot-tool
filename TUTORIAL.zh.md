# gstack 教程 #4 —— 从 AI 规划到可用的生产级软件

**读完之后你能做什么：** 像教程 #1-#3 教的那样运行 gstack，再加上一项新能力
—— 在独立会话中运行第二个、独立的 AI，它唯一的工作是把 gstack 的原始输出
翻译成你的下一条精确提示词；并且通过一个真实的完整案例，了解一个完整的
19 阶段 AI 原生开发体系从头到尾究竟是什么样子：从前提质疑到生产环境部署，
每一个有实质影响的步骤都设有人工关卡。

**所需时间：** 仔细读完本教程需要 3-5 小时；本文记录的项目（一个完整应用、
五轮复核加一次生产部署）在一次长会话中完成。规模扩大体现在每个阶段的实际
耗时上，而不是步骤数量的增加。

**前置条件：** gstack 已经可以正常运行（教程 #1-#3 已覆盖）；可以在另一个
窗口打开的第二个 AI 对话——任意提供方均可；运行示例应用需要 Node.js 和 npm。

English version: [`TUTORIAL.md`](TUTORIAL.md)

> **教育用途声明。** 本教程及其记录的示例应用（Tweet Screenshot Tool）仅供
> 教育目的使用——教授一套可验证的、AI 原生的开发流程。它不授予对示例应用
> 所展示的任何第三方内容的权利（推文内容仍属于其作者，展示方式与任何浏览器
> 或新闻文章的公开嵌入方式相同）。本文不构成法律意见。

---

## 第 0 部分 —— 心智模型

### 0.1 我们在构建什么

Tweet Screenshot Tool：粘贴一条推文链接，应用在服务端抓取内容，渲染出像素
级还原的实时预览，你可以自定义样式（背景、内边距、导出倍率、明暗主题），
然后导出为 PNG、剪贴板图片，或一个可分享链接——让另一个人打开后能还原相同
的推文和样式。第二种模式（`/thread`）对手动组装的推文列表做同样的事，导出
为一张合并图片。

V1 已在生产环境上线两种模式。刻意排除的功能：自动串联推文（没有免费数据源
支持这一点）、浏览器扩展、公开的无头生成 API——这些是明确声明的排除项，
不是被悄悄遗漏的缺口（`reviews/01-ceo-review.md`、`TODOS.md`）。

### 0.2 为什么"AI 写代码"是错误的心智模型

那句话描述的大约只是这个项目实际运行的十九个阶段中的一个。实际发生的是：

```
人类创始人 / 学习者
        |
外部 AI 导师 / 元智能体   <- 一个独立的 AI，独立会话
        |
优化后的提示词
        |
gstack 专家角色             <- CEO、PM、架构师、复核者、工程师……
        |
证据 / 产出物
        |
人工关卡
        |
下一个专业角色
```

规划、质疑、实现、测试、人工验证是不同的工作，由不同的角色完成，每一个都
产出下一个角色或人类可以实际核查的证据。教程 #1-#3 已经教过这套体系的
gstack 那一半（CEO/PM/架构师/QA/DevOps 加一条机械化的关卡链）。这里新增的
是它之上的一层：一个独立的第二 AI，它唯一的工作就是把 gstack 的原始输出
翻译成下一条精确指令——完整模式和四个有据可查的例子见
`docs/external_ai_mentor.md`，其中包括它纠正 gstack 自身框架错误的实例。

本教程走过的完整顺序：

```
0. 问题界定           6. 工程复核          12. 最低限度质检
1. AI CEO             7. 高风险验证性小试  13. 部署
2. AI 项目经理         8. AI 工程师        14. 生产环境冒烟测试
3. 架构                9. 证据检查点       15. 冻结 + 复盘
4. 安全 + UX          10. 浏览器 QA
5. 独立第二意见        11. 人机协同测试
```

这个完整顺序，经过泛化后可复用，完整写在
[`docs/GSTACK_REUSABLE_PROJECT_WORKFLOW.md`](docs/GSTACK_REUSABLE_PROJECT_WORKFLOW.md)。

**检查点 0**
- [ ] 你能说出这个项目涉及的两个 AI 系统，以及各自的职责
- [ ] 你理解"AI 写代码"只是 19 个阶段中的第 8 个，而不是整个流程

---

## 第 1 部分 —— AI CEO

**核心教训：** *在代码存在之前做出的架构和范围决策改动成本很低；代码写完
之后再改，成本就不低了。*

`/plan-ceo-review` 不是以功能列表开场，而是以前提质疑开场：是否已经存在可用
的免费替代方案？**经过实际验证，而非假设**——两个最接近的开源竞品都构建在
官方 X API 之上，而该 API 自 2026 年起已无免费层级。这一个经过核实的事实就
淘汰了所有现有竞品，并定下了整个架构：不使用付费 API，这也排除了"在一个
已死 API 上做分叉"和"服务端无头浏览器截图"这两种替代方案。

**本阶段锁定的决策：**

| 决策 | 理由 |
|---|---|
| `react-tweet` + `modern-screenshot`，Next.js 部署在 Vercel | 通过 X 的免费公共联合供稿端点渲染；无需付费 API，无需爬虫 |
| Thread 模式在复核过程中被重新定义：从自动发现改为手动 | 没有免费数据源能在不使用付费 API 或爬虫的情况下发现一条推文的下一条——这是在复核过程中就发现的，而不是等错误的东西造出来之后才发现 |
| 在任何代码存在之前就封堵了一个 SSRF 漏洞 | 按子字符串匹配主机名会放行 `evil-twimg.com.attacker.net`；在实现之前就修正为精确主机名匹配 |
| 8 项提议的范围扩展中接受了 6 项 | 导出优化、剪贴板复制、可分享链接、Thread 模式；两项被拒绝 |

**本阶段产出的证据：** `reviews/01-ceo-review.md`——一份 11 个章节、25 项
发现的复核报告，每一项都对应一个单独的决策，而不是批量通过。

**检查点 1**
- [ ] 你能说出塑造了整个架构的那一个经过实际验证的事实
- [ ] 你能说出一项在复核过程中中途改变的范围决策，以及原因

---

## 第 2 部分 —— AI 项目经理

**核心教训：** *任务拆解的可信度取决于它所依据的计划的质量——跳过这个角色
是一个真实的、可核查的缺口，不是可以免费跳过的一步。*

**明确声明的缺口，而非隐藏：** 本项目没有单独运行专门的 PM 角色。CEO 复核
自身的输出承担了这一职能——一份实现任务清单（T1-T14，按 P1/P2/P3 排优先级）
以及 `TODOS.md` 中对明确排除在范围之外的条目的记录。这在本项目中之所以行得
通，是因为 CEO 复核本身足够详尽，可以兼作任务拆解。这不是一个可以不加批判
地照搬的模式——在更大的代码库中，跳过专门的 PM 环节可能会导致真实的排序
错误，而这本应由专门的 PM 环节发现（`reviews/02-spec.md`）。

真正起到关键作用的那一次排序决策——在大规模实现之前插入一次验证性小试
（T1.5，见第 7 部分）——来自*工程*复核，而不是 PM 环节。这或许正是它该
出现的位置：由真实技术未知数驱动的排序决策，应当归属于发现这个未知数的
那次复核。

**检查点 2**
- [ ] 你能解释为什么"没有运行 PM 环节"被记录为明确声明的缺口，而不是被
      悄悄并入 CEO 复核的输出中

---

## 第 3 部分 —— 架构

**核心教训：** *架构应当用最少的组成部件，移除最重要的技术约束。*

### 3.1 推文数据路径

```
浏览器
  |
Next.js 应用
  |
/api/tweet/[id]              <- 仅服务端
  |
react-tweet
  |
X/Twitter 公共联合供稿端点
```

该联合供稿端点会阻止浏览器直接发起的 CORS 请求——这是针对真实端点实际验证
过的，不是假设。服务端中转在结构上是必需的，不是风格选择。`/api/tweet/[id]`
复用了 `react-tweet` 自身的服务端抓取模式，而不是自己造一个代理。

### 3.2 导出路径

```
渲染后的推文 DOM
  |
modern-screenshot
  |
捕获时的资源处理
  |
/api/image-proxy             <- 经过 SSRF 加固
  |
PNG
```

实时预览可以自由展示远程图片——跨域的 `<img>` 标签在任何浏览器中都能正常
渲染。导出则不同：把 DOM 转换成 canvas 需要*读取像素数据*，浏览器会为跨域
图片阻止这一操作，除非它通过浏览器信任的方式提供服务。这正是图片代理存在
的全部原因，也是它只在**捕获时**才有意义、实时预览完全不需要它的原因——
这一区别是在第 7 部分中通过艰难的方式发现的。

### 3.3 组件

| 组件 | 角色 |
|---|---|
| Next.js | App Router，TypeScript，部署在 Vercel |
| react-tweet | 通过 X 的免费公共联合供稿端点抓取 |
| modern-screenshot | DOM → PNG 导出，捕获时通过代理路由 |
| 服务端路由 | `/api/tweet/[id]`——仅服务端 |
| 图片代理 | `/api/image-proxy`——经过 SSRF 加固 |
| 基于 URL 的分享状态 | 自定义状态存在 URL 中，不落服务器 |
| 无数据库 | V1 没有账户体系，没有持久化层 |

一个能工作的组件不等于一套完整的产品架构——这里覆盖的是 V1 实际需要的
部分，不是一款成熟产品最终会拥有的一切（诚实的缺口清单见第 16 部分）。

**检查点 3**
- [ ] 你能解释为什么实时预览不需要代理，而导出需要
- [ ] 你能说出这套架构移除的、最少部件对应的那个约束

*来源：`README.md`"Architecture note"；`reviews/01`、`04`。*

---

## 第 4 部分 —— 安全与 UX 复核

**核心教训：** *只有正常路径能跑通，功能就还没做完。*

两者都在大规模实现之前完成复核，嵌入在 CEO 复核的第 3 节和第 11 节中，而
不是作为独立角色单独运行。

### 4.1 安全

| 控制措施 | 细节 |
|---|---|
| 精确主机名白名单 | `pbs.twimg.com`、`abs.twimg.com`、`video.twimg.com`——子字符串匹配被明确拒绝（它会放行 `evil-twimg.com.attacker.net`） |
| 仅限 HTTPS | 非 `https:` 的 URL 一律拒绝 |
| 重定向重新校验 | 每一次重定向跳转都会重新对照同一份白名单检查——盲目跟随重定向是经典的 SSRF 绕过手法 |
| 响应体积上限 | 10MB，通过响应头和下载后双重检查 |
| MIME 校验 | 仅接受 `image/*` 和 `video/*` 的内容类型 |
| 不转发凭据 | 代理从不向上游转发 cookie 或认证头 |

### 4.2 UX

| 关注点 | 解决方案 |
|---|---|
| 加载状态 | 一个推文形状的骨架屏，不是转圈动画 |
| Thread 部分失败 | 内联错误卡片；在失败推文被解决或移除之前，**导出被阻止**——绝不悄悄丢弃 |
| 重试 / 移除 | 每条失败的 thread 条目都可用 |
| 移动端 / 响应式 | 一个经过测试的基线，不是完整审计 |
| 无障碍 | 键盘可操作的控件、ARIA 标签、焦点状态——是基线，不是完整审计 |

**精确区分这三类失败很重要：** *安全性失败*是攻击者触及了不该触及的东西
（上面的 SSRF 类别）；*产品性失败*是软件在没有攻击者参与的情况下做错了事
（悄悄从导出中丢弃一条失败的 thread 推文）；*用户困惑*是软件做对了，但用户
无法理解（没有加载状态、没有错误提示）。这三者需要不同的修复方式——把它们
混为一谈会导致修错方向。

**检查点 4**
- [ ] 你能说出精确主机名白名单具体防止了哪种 SSRF 绕过手法
- [ ] 你能用本项目中的例子分别区分安全性失败、产品性失败、用户困惑

---

## 第 5 部分 —— 独立第二意见与跨模型张力

**核心教训：** *独立复核之所以有价值，是因为它能制造有建设性的分歧。*

"跨模型张力"不是复核者漏看的一个笔误——而是独立复核者发现两个看似合理的
决策*互相冲突*，或者更早批准的一个修复悄悄破坏了更晚批准的一项要求。
**明确声明的局限：** 这一轮复核是同模型、全新上下文的子代理执行的，不是
真正不同的模型或提供方——本环境中 Codex CLI 和 bash 均不可用。即便如此，
它仍然发现了一些同上下文复核很可能会漏掉的真实问题。

```
初始计划
  |
独立质疑
  |
矛盾 / 隐藏假设
  |
决策
  |
修订后的计划
```

| # | 张力 | 矛盾所在 | 解决方式 |
|---|---|---|---|
| OV-1 | 限流 vs. "无密钥" | Serverless 环境下的内存计数器每次冷启动都会重置——这样构建的限流器悄无声息地形同虚设，但真正的限流需要共享状态凭据，而这正是"无密钥"这条原则所排除的 | 修正为：真正的限流需要 Upstash/KV，后端出错时应故障开放 |
| OV-2 | Safari 剪贴板手势 | 已批准的剪贴板修复方案（等待字体、等待捕获，再写入）破坏了 Safari/iOS 要求 `clipboard.write()` 必须在与点击相同的同步手势链中触发这一规则 | `ClipboardItem` 直接包裹一个 `Promise<Blob>`，绝不预先 await |
| OV-3 | 视频导出未定义 | 计划里根本没说清"导出一条视频推文"对一个静态图片工具意味着什么 | 封面帧替换 + 播放图标徽章，让效果显得是刻意设计的 |
| OV-4 | Thread URL 长度无上限 | 一个真实 thread 的、经 URL 编码的分享状态天然没有上限 | 硬性上限约 12 条推文，明确提示，绝不静默截断 |
| OV-7 | 过早的共享组件假设 | 计划在任何 thread 代码存在之前就断言"单条推文和 thread 模式显然会共享 `TweetCanvas`" | 软化为一个明确的重新评估检查点，而不是被直接接受或拒绝 |

8 项张力中有 6 项被直接采纳，1 项（OV-7）被软化而非直接接受或拒绝，1 项在
已有独立验证证据的情况下被判定为低价值而跳过。如果第二个模型只是批准第一
个模型的输出，那它增加的不是复核层，而是延迟。

**检查点 5**
- [ ] 你能用自己的话解释是什么让 OV-1 成为一个真正的*矛盾*，而不仅仅是
      一个缺失的功能
- [ ] 你理解为什么"第二个模型同意了"本身不能算作任何东西的证据

*来源：`reviews/03-eng-review.md`（OV-1 至 OV-8）。*

---

## 第 6 部分 —— 工程复核

**核心教训：** *产品计划描述的是意图；工程复核检验的是这个意图能否被
实现。*

这里问题从"我们应该造什么"变成了"这份计划按现在写的样子，能否被安全、
干净地实现"。`/plan-eng-review` 依据外部 AI 导师起草的一份 12 项标准清单
运行（架构完整性、外部依赖风险、导出正确性、安全性、异步/数据流正确性、
组件/API 设计、实现顺序、测试策略、失败模式/可观测性、部署可行性、简洁性
/过度工程化、范围纪律）——而不是 gstack 自带的默认提示词。见
`docs/external_ai_mentor.md`。

| # | 发现 | 类别 |
|---|---|---|
| B1 | CEO 复核中的图片代理"修复"其实从未真正接到任何真实的 DOM 元素上——`react-tweet` 渲染的是纯粹的 `<img>` 标签，直接指向 `pbs.twimg.com`；计划中没有任何内容重新连接这一点 | **阻断项** |
| B2 | 代理加固不完整：未指定重定向重新校验、协议检查、体积上限、MIME 检查 | 实现决策 |
| B3 | 捕获前没有等待图片解码完成——与已经发现的字体竞态属于同一类静默错误输出问题 | 测试要求 |
| B4 | 限流器（来自 OV-1）没有定义失败行为——故障关闭的限流器会让 Upstash 故障变成整个应用的故障 | 实现决策 |
| B5 | 从未决定是按每条推文还是按整个 thread 来应用样式，这也影响 OV-4 中 URL 长度上限的计算 | 实现决策 |

**B1 是整个项目中最具实质影响的单一发现**——按计划实现的话，这个"修复"
根本不会被执行。这正是实现顺序发生改变的原因：**在任何大规模 Route
Handler 代码之前，插入了一次强制性的验证性小试（T1.5）**，专门用真实代码
而不是更多计划来验证图片代理接线的问题。结论：**在修复的前提下继续
（PROCEED WITH FIXES）**。

**检查点 6**
- [ ] 你能解释为什么 B1 是一个阻断项，而 B2/B4/B5 不是——尽管 B2 同样与
      安全相关
- [ ] 你能准确说出"一份计划从未真正接到真实代码上"对 B1 意味着什么

*来源：`reviews/03-eng-review.md`。*

---

## 第 7 部分 —— 高风险验证性小试

**核心教训：** *执行成功不等于输出正确。*

这是整份教程中最重要的一部分。**问题是：** `react-tweet` 渲染出的媒体资源
究竟是如何到达 `modern-screenshot` 的，远程媒体在导出时又该如何处理？B1
让这成为一个真实的、悬而未决的技术未知数——不是产品问题，而是"这到底能不
能跑通"的问题。

### 7.1 证据驱动的调查

不是靠文档或记忆，而是通过阅读已安装包的真实编译源码：

- `node_modules/react-tweet/dist/twitter-theme/*.js`——`EmbeddedTweet` 接受
  一个 `components` prop，可以覆盖头像和媒体的渲染方式；`TweetMediaVideo`
  渲染出一个真实的 `<video poster={...}>`，封面帧 URL 已经作为一个活跃
  属性存在。
- `node_modules/modern-screenshot/dist/index.mjs`——`fetchFn(url)` 会在
  *捕获期间*针对每一张远程图片被调用，消费方式与库自身的
  `responseType: "dataUrl"` 抓取路径完全一致。这个事实最终起到了极大
  作用。
- 同一轮源码阅读还发现了 `onCloneNode`/`onCloneEachNode` 钩子，它们会在
  *克隆出的* DOM 树被光栅化之前触发——这正是在不触碰真实、可交互页面的
  前提下，把 `<video>` 替换为其封面帧 `<img>` 的天然位置。
- **DOM 改写最终被证明完全不需要**，因为 `fetchFn` 只在捕获时拦截资源。
  实时预览继续指向真实的 `pbs.twimg.com` URL——没问题，因为跨域图片
  *展示*是正常的；CORS 只在 canvas 试图*读取*其像素时才会介入。只有
  捕获这一步需要经过 SSRF 加固的代理。

### 7.2 这次小试实际抓到的 bug

```
假设：      图片代理修复一旦接好线就应该直接能用
实验：      阅读库的真实源码；把 fetchFn 接到代理上
第一个结果： "成功" —— modern-screenshot 报告没有抛出异常
发现的 bug： 导出的 PNG 中，头像本该出现的位置显示为一个损坏图片图标
根本原因：   proxyAwareFetch 通过 URL.createObjectURL() 返回了一个
             blob: 对象 URL —— 这对普通的 <img src> 完全没问题，但
             modern-screenshot 会把克隆出的 DOM 序列化成一个 SVG 字符串，
             再把它当作一个全新的图片资源重新加载；来自当前文档的
             blob: URL 一旦被嵌入那个重新加载的 SVG 中，就无法可靠解析
修复：       改为返回一个自包含的 base64 data: URL，通过
             FileReader.readAsDataURL() 实现——这恰好匹配
             modern-screenshot 自身 responseType: "dataUrl" 的约定
视觉证明：   修正后的 PNG 被保存到磁盘并直接读取查看——真实的头像照片
             正确显示，圆形裁剪也正确，取代了原来的损坏图片图标。
             捕获耗时也随之下降（1046ms -> 468ms），是一个次要的
             佐证，证明一条更慢、失败的路径被替换掉了
```

那次第一次捕获中，除头像外的一切——文字、字体、渐变背景、圆角——都是正确
的。这是一个典型的静默错误输出问题的教科书案例：代码在任何朴素检查下都
"能跑"，却仍然产出了错误的结果。

**检查点 7**
- [ ] 你能准确解释为什么 `blob:` URL 在重新加载的 SVG 内部会失效，而在
      普通的 `<img src>` 中却能正常工作
- [ ] 你能说出"没有抛出异常"在这里实际证明了什么（以及它没能证明什么）

*来源：`reviews/04-qa-report.md`。*

---

## 第 8 部分 —— AI 工程师实现

**核心教训：** *构建能产出证据的、最小的垂直切片。*

实现是分阶段推进的（依据外部 AI 导师的 14 阶段指令——见
`docs/external_ai_mentor.md`），而不是一次性倾倒所有代码：基线、T1.5 小试、
真实 UI、交互正确性、thread 模式。

### 8.1 组件边界

| 组件 | 拥有 | 不拥有 |
|---|---|---|
| `TweetCanvas` | 渲染单条推文 | 抓取状态、thread 状态、导出状态 |
| `ExportPipeline`（`export-image.ts`） | 接收一个 DOM 节点并产出一张 PNG | 该节点代表的是一条推文还是整个 thread |
| `ThreadBuilder` | 有序的多推文行为：添加、重新排序、重试、移除 | 单条推文的渲染 |
| URL 状态（`style-state.ts`、`thread-state.ts`） | 仅负责序列化/反序列化 | 任何渲染或抓取逻辑 |

### 8.2 实际构建的内容

- 推文 URL 解析（`tweet-url.ts`）
- `/api/tweet/[id]`——服务端抓取，复用了 `react-tweet` 自身的模式
- `/api/image-proxy`——第 3-4 部分中经过 SSRF 加固的代理
- `TweetCanvas`、`CustomizePanel`、`ExportControls`、`TweetSkeleton`
- 过期响应保护：一对请求令牌 + `AbortController`——两个竞速请求中
  *更晚*的那个始终获胜，通过一次确定性的网络延迟测试验证，而不是靠运气
- 带版本号的 URL 状态，支持 `popstate` 前进/后退——针对真实 DOM 属性
  验证，此前发现浏览器自动化工具自身的 `back()`/`forward()` 封装不支持
  基于 pushState 的 SPA 路由（这是工具本身的局限，被发现并绕过，不是
  应用的 bug——见第 9 部分）
- `ThreadBuilder`：手动多 URL 构建器、每条独立抓取、重新排序、防重复、
  重试、移除、第 4 部分中的内联错误卡片、整个堆叠共享同一套样式

### 8.3 DRY 是一个假设，不是教条

OV-7（第 5 部分）标记出"TweetCanvas 显然会在单推文和 thread 模式之间共享"
是一个在任何 thread 代码存在之前就做出的未经验证的假设。它被软化为一个
重新评估检查点，而不是被直接接受或拒绝——在你手头还没有第二个用例可供
对照检验时，这是诚实的立场。

**检查点 8**
- [ ] 你能说出上面一个组件边界，并解释如果这条边界被打破会发生什么
- [ ] 你能用自己的话解释"DRY 是一个假设"是什么意思

---

## 第 9 部分 —— Aside AI 浏览器与真实浏览器 QA

**核心教训：** *真实浏览器行为是一层独立于代码正确性的证据。*

Aside——一款 AI 原生的真实浏览器——已经在这台 Windows 11 机器上从之前的
会话中安装完毕，其命令行自动化接口也一并装好。明确的规则（由外部 AI 导师
起草，而不是一条一次性的用户偏好）是：*"所有交互式浏览器工作都使用 Aside
作为主浏览器；不要安装单独的 Chromium；如果 Aside 失败，应停下并报告原因，
而不是在未经明确批准的情况下自动切换浏览器引擎。"* 这条规则在真实压力下
得到了坚持——一次被拒绝的 `npm install -D playwright` 调用在会话中途被放弃，
转而使用已经验证过的 Aside CLI，而不是被悄悄绕过。

三个不同的证据层，彼此不可互相替代：

| 层级 | 验证的内容 |
|---|---|
| 自动化测试 | 孤立环境中的确定性逻辑 |
| AI 浏览器 QA（Aside） | 真实浏览器中的真实应用行为 |
| 人工浏览器测试 | 可用性与主观正确性感受 |

从 T1.5 小试到最终生产环境冒烟测试的每一个检查点都通过 Aside 完成：注入
网络延迟以确定性地触发过期请求竞态（第 8 部分）；对照真实 DOM 属性检查
真实的 `window.history.back()/forward()`——此前发现 Aside 自身对某个组件
`aria-pressed` 的无障碍快照渲染不可靠（这是一个在会话中途被发现并处理的
工具本身的小问题，不是真正的应用 bug）；真实下载的 PNG 被读取并直接进行
视觉检查——这正是第 7 部分中实际抓到头像 bug 的方式。

**检查点 9**
- [ ] 你能说出决定使用哪种浏览器引擎的明确规则，以及它被真正考验的那
      一个时刻
- [ ] 你能说出本项目中 Aside 自身工具出过的一个错误，以及团队是如何把它
      和真正的应用 bug 区分开的

---

## 第 10 部分 —— 人机协同的人工测试

**核心教训：** *AI 可以产出证据；由人类决定这些证据是否足够充分。*

这一部分由用户报告，而非 AI 可独立验证——**这正是它的意义所在。**

**本地测试：** 打开 PowerShell，运行 Next.js 开发服务器，手动在 Aside 中
打开应用，测试单推文和 thread 两种界面，导出一张 PNG，打开下载的文件，
进行视觉检查。

**生产环境测试：** 打开已部署的 Vercel 应用，重复核心流程，视觉确认结果。

这一步骤带来的是任何自动化都无法产出的东西：主观视觉质量感受（"这看起来
对不对？"）、产品是否*感觉*可用而不仅仅是功能上能跑、对导出产物是否真的
值得分享的信心，以及那些具有实质影响的批准决策——包括本项目中两个只能由
人类执行、也应当由人类执行的关卡：Vercel 账户认证，以及上线决策。

**检查点 10**
- [ ] 你能说出本项目中人工测试带来的、任何 AI 驱动检查都无法产出的三件
      事情
- [ ] 你能说出本项目中两个只能由人类执行的授权关卡

---

## 第 11 部分 —— 证据阶梯

**核心教训：** *证据越接近真实用户和真实输出，就越有力。*

从弱到强的十个层级，以本项目实际爬升的顺序排列：

| 层级 | 它实际消除了哪方面的不确定性 |
|---|---|
| 1. 想法 | 还谈不上可行性 |
| 2. 计划 / 架构假设 | 只是意图，不是事实 |
| 3. 代码可编译 / 可构建 | 仅类型正确性 |
| 4. 自动化测试 | 孤立函数行为——**本项目未编写，是明确声明的缺口** |
| 5. 本地浏览器"跑通了" | 看起来像证据——通常不是。**头像 bug 恰恰藏在这一层** |
| 6. 真实浏览器行为（Aside，确定性设置） | 一个特定的、原本难以复现的失败，被变得可复现 |
| 7. 导出产物经过视觉检查 | "没有报错"不等于"输出正确"——**正是这一层抓到了头像 bug** |
| 8. 人工视觉检查 | 自动化检查共有的盲点 |
| 9. 生产环境部署 | "在开发环境能跑"与"在真实基础设施上能跑"的差别 |
| 10. 生产环境人工测试 | *真正上线的东西*是否经得住考验 |

**本项目自身历史证明的规则：** "捕获函数成功返回"作为证据，远弱于"一个
人对导出的 PNG 进行了视觉检查"。把"返回时没有抛出异常"当作验证的*起点*，
而不是终点——在把一个可见输出的功能称为已验证之前，至少要爬到第 7 层。

教程 #3 教的是一条机械化的关卡链（T1-T5，通过/失败/VOID）作为其验证模型，
适合有确定性机械检查手段的主题。而本项目的真实风险——一个按构造无法被
通过/失败脚本捕获的静默错误输出 bug——正是本教程改教"证据阶梯"而不是
关卡链的原因：这是一个关于一项声明*需要多少*验证，而不只是某项检查是否
通过的框架。

**检查点 11**
- [ ] 你能说出你自己最近一次"它能用了"的声明实际达到了证据阶梯的哪一层
- [ ] 你能解释为什么第 5 层恰恰是陷阱所在

*来源：`reviews/04-qa-report.md`。*

---

## 第 12 部分 —— 调试何时变成浪费

**核心教训：** *如果实验没有在缩小不确定性，坚持就不是进展。*

真实事件：一条 thread 分享链接的样式预设有时无法在首次加载时恢复。通过
临时插桩，在每次渲染时直接确认 React 状态是正确的——`style.backgroundId`
确实是 `"midnight"`——而渲染出的 DOM 中 `aria-pressed` 属性却仍然显示默认
值。这是一次真实的"状态与 DOM 不一致"问题，而不是解码器或组件代码中的
逻辑错误（两者都被单独验证为正确）。它在切换 Strict Mode、完全清空
Turbopack 缓存、重启开发服务器之后依然存在。这占用了本项目中单次调查耗时
最长的一段时间。

**更好的调试模型——检查最可能出错的那一层，而不是第十层：**

```
URL
  |
解码器
  |
水合（hydration）
  |
React 状态
  |
组件 props
  |
DOM
  |
浏览器观察
```

**在每一层的规则：** 通过 → 不再改动这一层，转向下一层。失败 → 先调查
*这一层*，不要跳到更后面。

**时间盒原则：** 如果大约 20-30 分钟的调试没有实质性地缩小假设空间，就
停止继续加日志。把实验做得更小。孤立出一个边界。正确的做法应该是在这次
调查*开始之前*就设好时间盒，而不是消耗了这么多工具调用之后才停下来——
最终停下并记录下来的决定是对的，但应该更早做出。

**检查点 12**
- [ ] 你能解释为什么"React 状态被确认是正确的"没有终止这次调查
- [ ] 你能用一句话说出时间盒原则

*来源：`reviews/05-ship.md`（"时间花在了哪里"）。*

---

## 第 13 部分 —— 快速收尾

**核心教训：** *上线速度来自于选择不去调查什么。*

从"调查一切"到"修复上线关键问题、记录其余问题、然后上线"的刻意转变。

| 修复 | 推迟 |
|---|---|
| 构建失败 | 细枝末节的样式问题 |
| 崩溃 | 投机性的重构 |
| 核心导出失败 | 完美的测试覆盖率 |
| 严重的安全问题 | 罕见的非核心边缘情况 |
| 明显损坏的核心交互 | V2 功能 |

具体做法：`TODOS.md` 明确记录了验证债务（已实现但未验证 / 已知 bug /
刻意推迟，分别标注，而不是混为一谈）；诊断代码被移除；脚手架模板残留
（页面标题、元数据、README）被清理；跑了一次最终的生产构建；用一次快速
的 Aside 冒烟测试取代了完整的重新审计。

这次模式切换源于外部 AI 导师的两次干预（完整细节见
`docs/external_ai_mentor.md`）：首先，看到第 12 部分调试会话跑了很久却没有
孤立出根本原因的记录，导师说*"我现在会停止当前这次调试，重置调试方法"*，
并起草了一套五层二分查找协议。**值得诚实地把接下来实际发生的事讲清楚：**
人类并没有运行那个具体的协议。当时人机协同测试（第 10 部分）已经完成，
用户转而要求一条更快的"尽快收尾这个项目"提示词——这就产生了本部分遵循的
Ship Mode 指令。那个具体的协议没有被沿用；但其背后的原则（停下、设时间盒、
记录而不是穷追）被沿用了。

**检查点 13**
- [ ] 你能把三个假设性的 bug 分别归入"立即修复"还是"推迟并记录"
- [ ] 你能解释为什么导师提出的具体协议没有被采用，并不削弱这次干预的
      价值

---

## 第 14 部分 —— 部署与人工授权关卡

**核心教训：** *即便在 AI 原生的工作流中，具有实质影响的授权依然是人工
关卡。*

```
Agent 准备部署
  |
人类授权账户访问
  |
Agent 执行部署
  |
AI 浏览器验证生产环境
  |
人类验证生产环境
```

Vercel CLI 没有全局安装；全程使用 `npx vercel`——无需永久安装。人类在自己
的终端中运行了 `npx vercel login`；授权在 Aside 中手动完成；`npx vercel
whoami` 确认成功后，控制权才交还给 AI。从那之后，AI 自主完成了余下的部署
流程：确认认证状态、执行最终生产构建、创建 Vercel 项目、执行部署。Vercel
自身的既定行为是：新项目的首次部署总是直接成为生产环境——不存在一个单独的
预览 URL 可供先行把关，因此生产环境冒烟测试是立即针对线上 URL 进行的。

**本部分刻意不展示的内容：** 真实用户名、团队名称、设备授权代码、或
OAuth URL。这些在本教程写作之前就已经过审查并被排除——见
`docs/TUTORIAL_4_PRIVACY_REVIEW.md`。*流程本身*才是教学内容，具体账户
不是。

**检查点 14**
- [ ] 你能说出在整个部署流程中，唯一一个 AI 不能也不应该执行的动作
- [ ] 你能解释为什么"首次部署即成为生产环境"改变了验证顺序

---

## 第 15 部分 —— 生产环境结果

**核心教训：** *生产环境的证据比 localhost 上的信心更有分量。*

**已验证的生产环境流程：**

- **单条推文：** 加载一条推文 → 自定义 → 导出 PNG / 剪贴板 / 分享链接
- **Thread：** 加载多条推文 → 导出为一张合并的 PNG

两者都通过真实浏览器自动化（Aside）确认，并由人类报告为已针对已部署的
线上 URL 手动测试过，与第 10 部分的本地测试是分开进行的。在已测试的流程
中未观察到致命的控制台错误。

**本教程不做的声明：** 视频推文导出、emoji 渲染、多图推文已经实现，但本次
会话中未经过实证验证——见第 16 部分。未经测试的行为不会被声称为可用；
而是被明确标注为未测试。

**检查点 15**
- [ ] 你能说出本项目在生产环境中实际验证过的两个流程，以及明确声明未
      验证的部分

---

## 第 16 部分 —— 已知局限与验证债务

**核心教训：** *已知局限是可以接受的，前提是它们被明确说明、有边界，并且
与发布目标兼容。*

| 项目 | 状态 |
|---|---|
| 两个公共 API 路由上的限流 | 已计划，复核过两次（OV-1、B4），代码中从未实现 |
| Thread 分享链接样式恢复 | 调查过一轮，有边界（第 12 部分）；未找到根本原因 |
| 视频 / emoji / 多图导出 | 已实现，未经实证验证 |
| Safari / iOS 剪贴板 | 已实现，未在真实 Safari 上经过实证验证 |
| 自动化测试套件 | 尽管计划中有明确要求，却未编写——一个明确声明的流程缺口 |

**四个清晰的类别，而不是混成一团：** *已实现但未验证*（剪贴板、视频/emoji/
多图导出——代码路径存在，但还没有人用真实数据实际跑过）；*已知 bug*
（thread 样式恢复问题）；*刻意推迟的功能*（限流——这是一个 SSRF 加固覆盖
不到的真实缺口，对于教程规模的演示而言可以接受，但不适用于真实的公开
流量）；*规模化之前必须具备的安全控制*（同样是限流，明确声明而非暗示
已经就绪）。

以上没有一项阻碍了教程 #4 V1 的发布，因为它们都没有触及第 15 部分中已经
验证过的两条核心流程，并且每一项都在此处被明确说明，而不是等读者自己
艰难地发现。

**检查点 16**
- [ ] 你能把上面每一项归入正确的类别
- [ ] 你能解释为什么"限流被复核过两次"不等于"跳过限流是安全的"

*来源：`TODOS.md`；`reviews/05-ship.md`。*

---

## 第 17 部分 —— 可复用的 gstack 项目操作系统

**核心教训：** *真正可复用的资产不是代码——而是产出代码的那套开发体系。*

完整的 16 阶段流程（第 0 部分图示的展开版，每个阶段附有目的、退出标准、
人工关卡），完整写在
[`docs/GSTACK_REUSABLE_PROJECT_WORKFLOW.md`](docs/GSTACK_REUSABLE_PROJECT_WORKFLOW.md)——
足够简洁，真正可以在你的下一个项目中复用，而不只是拿来欣赏。在打开那份
文档之前，有两点结构性的提醒值得带上：

- **让团队结构匹配项目规模。** 本项目只运行了一轮独立领域复核（第 5
  部分），而不是两轮；并且跳过了专门的 PM 角色（第 2 部分），因为 CEO
  复核已经覆盖了这一职能。复制的应该是这种*判断力*，而不是精确的角色数量。
- **导师模式的可泛化性在这一个项目之外尚未得到证明。**
  `docs/external_ai_mentor.md` 中的四个有据可查的纠正实例足以说明这个
  模式，但不足以证明它在任何项目上都必然物有所值。在盲目信任它之前，先
  在你自己的下一个项目上测试一下。

**检查点 17**
- [ ] 你已经打开过 `docs/GSTACK_REUSABLE_PROJECT_WORKFLOW.md`，并能说出
      其中高风险验证性小试阶段的退出标准

---

## 第 18 部分 —— 最终教训

本项目真正换来的十条教训，而不是十条泛泛的最佳实践：

1. 先规划再写代码，但不要过度规划——CEO 复核只跑了一轮，不是五轮。
2. 按决策类型划分不同的 AI 角色：产品判断、技术可行性、正确性验证不是
   同一个问题。
3. 独立复核应当质疑假设，而不仅仅是批准初稿——见第 5 部分四个真实的
   矛盾。
4. 在大规模实现之前，用一次强制性的验证性小试尽早测试风险最高的假设——
   B1 和 T1.5 小试（第 6-7 部分）。
5. 一次函数调用返回成功，产出物仍然可能是错的——头像 bug 就是本项目的
   证明，不是一个假设情形。
6. 真实浏览器证据很重要，它是独立于自动化测试和人工判断的一层，不能
   替代任何一方，也不能被任何一方替代。
7. 人工视觉判断仍然不可替代——本项目中没有任何自动化检查能确认"这看起来
   对不对"。
8. 已知局限不会自动阻碍上线——它们需要明确、有边界，并且在发布目标的
   核心流程之外。
9. 人类始终保留具有实质影响的授权和上线决策，本项目没有任何例外。
10. 收尾是一项工程技能——第 13 部分的快速收尾是一次刻意的模式切换，不是
    放弃。

---

## 故障排查

| 症状 | 可能原因 | 最简响应 |
|---|---|---|
| 3000 端口已被占用 | 另一个 `npm run dev` 仍在运行 | Windows 上用 `taskkill /PID <pid> /F`，或改用工具建议的端口 |
| `vercel` 命令无法识别 | Vercel CLI 未全局安装 | 每次 Vercel 命令都用 `npx vercel`——无需永久安装 |
| Aside CLI 在 PATH 中找不到 | 刚安装完，当前 shell 的 PATH 尚未更新 | 重新打开终端，或把 CLI 安装目录显式加入 PATH |
| 浏览器自动化说一套，人一看是另一套 | 自动化和人工测试验证的是不同的东西（第 9 部分） | 不要单独信任任何一方；就具体的那条结论重新手动核查 |
| 捕获"成功"了，但导出的图片是错的 | 静默错误输出——见第 7 部分 | 在信任一次捕获之前，先把证据爬到第 11 部分的第 7 层 |
| 会话中途上下文/token 用量偏低 | 长时间调查、冗长的工具输出 | 明确记录进度检查点；不要让一次没有设时间盒的调试会话（第 12 部分）耗尽整个预算 |
| 某份源文档看起来过时或与代码矛盾 | 文档写于后续修复之前 | 优先信任代码和复核文件，而非文字描述；把它记为一个文档修复项 |
| 中英文教程似乎有偏差 | 没有运行机械化检查 | 在信任任何一种语言版本之前，先运行结构一致性脚本——见 `docs/TUTORIAL_4_DOCUMENT_MANIFEST.md` |
| 幻灯片或文档"构建成功"但看起来不对 | 构建干净不等于渲染正确（第 11 部分的教训，反过来用在了本教程自己的工具链上） | 在称它完成之前，先渲染并检查实际输出 |

---

## 值得保留的纪律

- 证据高于信心——去爬阶梯，而不只是打个勾。
- 找到能反证你假设的最小实验。
- 高风险验证性小试排在最前面，而不是最后。
- 明确声明验证债务；不要把"未测试"和"能用"混为一谈。
- 按决策类型划分 AI 角色，规模匹配项目本身。
- 人工授权关卡不容商量，每一次都是。
- 发布前先做隐私复核，而不是发布之后。
- 机械化地验证双语一致性——不要凭肉眼信任它。
- 渲染并检查每一个生成的产出物——干净的构建只是第 3 层，不是第 7 层。
- 达到发布目标就停下；不要为了追求完整而无限延伸。

---

## 附录 A —— 命令参考

只列出复现本项目实际有用的命令。以下任何地方、或本教程的任何其他部分，
都不出现真实凭据或账户标识——见 `docs/TUTORIAL_4_PRIVACY_REVIEW.md`。

```bash
# 本地安装与运行
npm install
npm run dev              # http://localhost:3000  （单条推文）
                          # http://localhost:3000/thread （thread 模式）

# 构建与类型检查
npm run build
npx tsc --noEmit
npm run lint

# 部署（无需全局安装）
npx vercel login         # 打开浏览器进行账户授权
npx vercel whoami         # 部署前确认认证状态
npx vercel                # 预览部署
npx vercel --prod         # 生产环境部署

# Aside 就绪探测（概念性示意——见 docs/external_ai_mentor.md）
aside repl 'console.log("ASIDE_READY " + pwd)'
```

## 附录 B —— 双语术语表

| English | 中文 |
|---|---|
| AI CEO | AI CEO |
| AI Project Manager | AI 项目经理 |
| AI Architect | AI 架构师 |
| Security Review | 安全复核 |
| UX Review | 用户体验复核 |
| Second Opinion | 第二意见（外部意见） |
| Cross-model tension | 跨模型张力 |
| Engineering Review | 工程复核 |
| High-risk spike | 高风险验证性小试 |
| Evidence Ladder | 证据阶梯 |
| Human-in-the-loop | 人机协同（人工参与） |
| Browser QA | 浏览器 QA |
| Verification debt | 验证债务 |
| Minimal QC | 最低限度质检 |
| Ship gate | 上线关卡 |
| Meta-agent / External AI Mentor | 元智能体 / 外部 AI 导师 |

## 附录 C —— 完成定义

- [x] 英文教程（`TUTORIAL.md`）覆盖全部 19 个阶段，每个部分都有核心教训
      与检查点
- [x] 中文教程（`TUTORIAL.zh.md`）逐节对应
- [x] 结构一致性经过机械化验证，而非凭肉眼判断
      （`_build/verify_tutorial_parity.py`）
- [x] 隐私复核完成——`docs/TUTORIAL_4_PRIVACY_REVIEW.md`、
      `docs/TUTORIAL_4_DOCUMENT_PRIVACY_CHECK.md`
- [x] 英文 Word 文档（`dist/gstack-tutorial-4_EN.docx`）已构建
- [x] 中文 Word 文档（`dist/gstack-tutorial-4_ZH.docx`）已构建
- [x] Word 文档已渲染并经过视觉检查，而不只是构建完成——自动化 COM 渲染
      在本环境中挂起（该受阻情况记录在
      `docs/TUTORIAL_4_DOCUMENT_PRIVACY_CHECK.md` 中）；一位人类在
      Microsoft Word 中直接打开并审阅了 `dist/gstack-tutorial-4_EN.docx`
      和 `_ZH.docx` 两份文档，确认均正常——这是更有力的证据层级，构成
      本项检查的最终完成依据
- [x] Word 文档已对渲染输出进行隐私扫描（`_build/verify_doc_privacy.py`）
- [x] `docs/GSTACK_REUSABLE_PROJECT_WORKFLOW.md` 已完成
- [x] `docs/GSTACK_TUTORIAL_4_OUTLINE.md` 已完成
- [x] `docs/TUTORIAL_4_DOCUMENT_MANIFEST.md` 已完成
- [x] 本教程中不再残留任何"尚未撰写"的过时声明
- [x] 本教程及其配套文档中不出现任何私人标识信息（用户名、主机名、
      账户/团队名称、设备代码）
- [x] 已知局限记录在第 16 部分，未被隐藏
